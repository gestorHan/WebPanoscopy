import * as React from "preact";
import * as ReactDOM from "preact";
import { h, Component } from "preact";



function PixSelector(props) {
    return (
    <div>
        {
//            (props.file)?
 //       <img src={props.file} />:<div/>
        }
    </div>
    )
  }

  